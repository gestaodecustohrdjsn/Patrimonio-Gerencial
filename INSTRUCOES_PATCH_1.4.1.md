# Patch Frontend 1.4.1

Este patch corrige apenas um erro de sintaxe introduzido na v1.4:
- dois trechos `async async function` no `app.js`.

Não há alteração de API nem de banco.

## Publicação
Pode subir por cima da versão 1.4.

O runtime mudou para:
`js/runtime-v141/`

Depois:
1. aguarde o GitHub Pages;
2. faça `Ctrl + F5`;
3. confirme `build 1.4.1` na tela de login.
